<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GameHokAssignment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="android:textColorPrimary">@color/textgrey</item>
        <item name="colorSecondary">@color/green2</item>
        <item name="tabIndicatorColor">@color/green3</item>
    </style>
</resources>